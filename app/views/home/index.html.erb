<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>

Unmarked tasks
<div id="oldUnmarkedTasks" class="carousel slide" data-interval="false">
  <div class="carousel-inner">
    <% active = true %>
    <% @unmarked.each do |task| %>
    <div class="carousel-item <% if active %>active<% active = false %><% end %>">
      <div class="card">
        <div class="card-header">
          <% if task.date == Date.yesterday %>
          Yesterday
          <% elsif task.date > (Date.today - 7) %>
          <%= task.date.strftime("%A") %>
          <% else %>
          <%= task.date %>
          <% end %>
        </div>
        <div class="card-body">
          <h5 class="card-title"><%= render_markdown(task.name) %></h5>
          <p class="card-text">
            <div class="list-group">
            <a href="#" class="list-group-item list-group-item-action" data-action="mark-done" data-item-id="<%= task.id %>"><i class="fa fa-check fa-fw"></i> I completed this task</a>
            <a href="#" class="list-group-item list-group-item-action" data-action="mark-rescheduled" data-item-id="<%= task.id %>"><i class="fa fa-chevron-right fa-fw"></i> Reschedule this task</a>
            <a href="#" class="list-group-item list-group-item-action" data-action="mark-progressed" data-item-id="<%= task.id %>"><i class="fa fa-step-forward fa-fw"></i> I progressed on this task</a>
            <a href="#" class="list-group-item list-group-item-action list-group-item-light" data-action="mark-cancelled" data-item-id="<%= task.id %>"><i class="fa fa-times fa-fw"></i> Cancel this task</a>
          </div>
          </p>
        </div>
      </div>
    </div>
    <% end %>
  </div>
  <a class="carousel-control-prev" href="#oldUnmarkedTasks" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#oldUnmarkedTasks" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>

  </a>
</div>

<hr>

Today

<%= render partial: "shared/cards", locals: { items: @items } %>

<hr>

Backlog

<%= render partial: "shared/cards", locals: { items: @backlog } %>
